// @flow

import type { ComponentType } from 'react';

export type Options = {
  renderComponent: ComponentType<any>,
};
export type Props = {
  onPageChange: Function,
  location: any,
  history: any,
};

export type State = {
  page: number,
};

// @flow

import type { ComponentType } from 'react';

export type Options = {
  renderComponent: ComponentType<any>,
};
export type Props = {
  pagination: {
    page: number,
    total: number,
    perPage: number,
  },
  onPageChange: Function,
  location: any,
  history: any,
};

export type State = {
  page: number,
};

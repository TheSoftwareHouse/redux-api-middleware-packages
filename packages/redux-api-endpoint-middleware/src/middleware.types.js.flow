// @flow

export type Config = {|
  apiUrl: string,
  excluded: string[],
|};

export type Options = {
  apiUrl?: string,
  additionalApiUrls?: {
    [key: string]: string,
  },
  excluded?: string[],
};

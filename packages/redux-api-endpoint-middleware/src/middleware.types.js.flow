// @flow

export type Config = {|
  apiUrl: string,
  excluded: string[],
|};

type ApiOptions = {
  apiUrl: string,
  [key:string]: any,
};

export type Options = {
  apis?: {
    default: ApiOptions,
    [key: string]: ApiOptions,
  },
  excluded?: string[],
};
